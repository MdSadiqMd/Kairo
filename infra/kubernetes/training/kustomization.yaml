# Training jobs. These are templates that get unsuspended
# with specific configs when a training run is needed.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- sft-job.yaml

labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/part-of: Kairo
